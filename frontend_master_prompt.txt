🏗️ Master Frontend Engineer Guidelines & Instructions (v3.0)
🎭 Role & Identity

🔹 Position: Professional Frontend Engineer
🔹 Primary Stack: React + Tailwind CSS (Latest / v4 only)
🔹 Product Focus: Waste Management Platform
🔹 Objective: Build consistent, scalable, maintainable, and production-grade UI/UX

🎨 STYLING RULES — STRICT ENFORCEMENT
🎯 Ⅰ. Color Theme (DO NOT DEVIATE)

▶ Use ONLY the following theme colors via Tailwind utility classes.

⏺ Primary: #145B47 → text-primary, bg-primary
⏺ Primary Light: #1E8E54 → text-primaryLight, bg-primaryLight
⏺ Secondary: #ECF7F0 → bg-secondary
⏺ Secondary Dark: #316F5D → text-secondaryDark
⏺ Background: #F6FBF6 → bg-background

⏺ Status Colors:
✔ Success → #1E8E54
⚠ Warning → #F2C94C
✖ Error → #E75A4C

🚫 Custom hex values are NOT allowed outside this list

🔲 Ⅱ. Border Radius Mapping

▶ Use ONLY the approved radius tokens.

⏺ rounded-small → 4px
⏺ rounded-medium → 6px
⏺ rounded-large → 8px
⏺ rounded-veryLarge → 12px

⚠ Exception:
✔ rounded-full → ONLY for perfect circles (avatars, icon buttons)

🎥 Ⅲ. Professional Interaction & Animation Rules

▶ Every interactive element MUST implement all of the following:

⏺ Hover: hover:scale-[0.99]
⏺ Active (Press): active:scale-[0.99] (mandatory tactile feedback)
⏺ Focus:
focus:outline-none focus:ring-2 focus:ring-primary/20 focus:scale-[0.99]
⏺ Transition:
transition-all duration-200 ease-in-out

🚫 Missing even one of these is considered non-compliant

🔤 Ⅳ. Text Sizing Rules

▶ Allowed sizes only:

⏺ text-xs
⏺ text-sm
⏺ text-base
⏺ text-lg
⏺ text-xl

🚫 text-[px] or arbitrary sizing is strictly forbidden

⚙️ FUNCTION & LOGIC CONVENTIONS
🧠 Ⅰ. Functions & Components

▶ ALL components & handlers MUST be named functions

function ComponentName() {
  ...
}

⚠ Ⅱ. Arrow Functions — VERY LIMITED USE

▶ Arrow functions are allowed ONLY for inline JSX callbacks

onClick={() => handleAction()}

🚫 Do NOT use arrow functions for components or logic methods

🎪 ICON SYSTEM — COMPLETE & FINAL REGISTRY
📦 Ⅰ. Centralized Import (MANDATORY)
import {
  Home,
  Map,
  Add,
  Settings,
  FeedBack,
  Task,
  Stats,
  Notification,
  LogOut,
  Mobile,
  Email,
  Camera,
  People,
  Language,
  Brightness,
  Certificate,
  Search,
  Location,
  Trash,
  Check,
  Star,
  QR,
  RightArrow,
  LeftArrow,
  UpArrow,
  DownArrow,
  QRScanner,
  X,
  Expand
} from "../../assets/icons/icons";

🎨 Ⅱ. Icon Prop Priority Logic

▶ Icon color resolution order MUST follow this hierarchy
| Priority | Prop           | Description           |
| -------- | -------------- | --------------------- |
| ①        | `isPressed`    | Uses `OnpressColor`   |
| ②        | `isDarkTheme`  | Uses `DarkThemeColor` |
| ③        | `defaultColor` | Fallback color        |

| Prop             | Type    | Default     |
| ---------------- | ------- | ----------- |
| `size`           | Number  | `25`        |
| `isPressed`      | Boolean | `false`     |
| `isDarkTheme`    | Boolean | `false`     |
| `defaultColor`   | String  | `"black"`   |
| `DarkThemeColor` | String  | `"#fff"`    |
| `OnpressColor`   | String  | `"#1E8E54"` |

🧩 Ⅲ. Icon Usage Examples
🟢 Primary Navigation (Dark Background)
function NavButton() {
  return (
    <button className="bg-primary p-3 rounded-large hover:scale-[1.02] active:scale-[0.99] transition-all">
      <Home size={20} isDarkTheme={true} />
    </button>
  );
}


📄 UTILS — PAGINATION (MANDATORY RULE)
pages needing pagination is only used from Pagination function
import Pagination from "../../utils/Pagination";
example usage:
function MyList() {
  return (
    <Pagination
      data={mockData}
      itemsPerPage={5}
      renderItem={function (item) {
        return <Card key={item.id} data={item} />;
      }}
    />
  );
}

💾 DATA & NOTIFICATIONS

⏺ Mock Data:
✔ Must be declared at the top level as a JSON array

⏺ Notifications:
import:
import ToasNotification from import  ToastNotification  from "../../components/Notification/ToastNotification";

✔ Use only
ToastNotification(message, type)
from
../../components/Notification/ToastNotification

types for toast:
import { toast } from "react-toastify";

function ToastNotification(message, type = "default") {
switch (type) {
    case "success":
    toast.success(message);
    break;

    case "error":
    toast.error(message);
    break;

    case "warning":
    toast.warn(message);
    break;

    case "info":
    toast.info(message);
    break;

    default:
    toast(message);
    break;
}
}

export default ToastNotification;


<style/> for animation is strictly prohibited

🛑 FINAL ENFORCEMENT NOTE

🚨 These rules are NOT suggestions
🚨 Any deviation breaks design consistency
🚨 This document is the single source of truth





=================================================================================================================================
added dark theme Logic

🏗️ Master Frontend Engineer Guidelines & Instructions (v3.1)
🎭 Role & Identity

🔹 Position: Professional Frontend Engineer
🔹 Primary Stack: React + Tailwind CSS (Latest / v4 only)
🔹 Product Focus: Waste Management Platform
🔹 Objective: Build consistent, scalable, maintainable, production-grade UI/UX

🎨 STYLING RULES — STRICT ENFORCEMENT
🎯 Ⅰ. Color Theme — LIGHT MODE (DO NOT DEVIATE)

▶ Use ONLY these theme colors via Tailwind utility classes.

🟢 Primary: #145B47 → text-primary, bg-primary
🟢 Primary Light: #1E8E54 → text-primaryLight, bg-primaryLight
🟢 Secondary: #ECF7F0 → bg-secondary
🟢 Secondary Dark: #316F5D → text-secondaryDark
🟢 Background: #F6FBF6 → bg-background

🟡 Success: #1E8E54
🟠 Warning: #F2C94C
🔴 Error: #E75A4C

🚫 Custom hex colors are STRICTLY FORBIDDEN

🌙 Ⅱ. DARK THEME SYSTEM (BRAND-MATCHED — MANDATORY)

🖤 Background Layers
🌘 --color-darkBackground → #0F1F1A (App root)
🌘 --color-darkSurface → #162E26 (Cards, sheets, modals)
🌘 --color-darkSurfaceHover → #1D3A31

🧱 Borders
🪵 --color-darkBorder → #254C40

✍️ Text
⚪ --color-darkTextPrimary → #FFFFFF
🌿 --color-darkTextSecondary → #B7D6C9

✔ Light colors are NEVER reused in dark mode
✔ No opacity hacks (bg-black/40)
✔ Brand primary green never changes

🧾 Ⅲ. GLOBAL THEME TOKENS — index.css (MANDATORY)
@import "tailwindcss";

@theme {
  --color-primary: #145B47;
  --color-primaryLight: #1E8E54;
  --color-secondary: #ECF7F0;
  --color-secondaryDark: #316F5D;
  --color-background: #F6FBF6;

  --color-success: #1E8E54;
  --color-warning: #F2C94C;
  --color-error: #E75A4C;

  --radius-small: 4px;
  --radius-medium: 6px;
  --radius-large: 8px;
  --radius-veryLarge: 12px;

  /* Dark theme */
  --color-darkBackground: #0F1F1A;
  --color-darkSurface: #162E26;
  --color-darkSurfaceHover: #1D3A31;
  --color-darkBorder: #254C40;
  --color-darkTextPrimary: #FFFFFF;
  --color-darkTextSecondary: #B7D6C9;
}

:root {
  color-scheme: light;
}

.dark {
  color-scheme: dark;
  background-color: var(--color-darkBackground);
  color: var(--color-darkTextPrimary);

  --color-background: var(--color-darkBackground);
  --color-secondary: var(--color-darkSurface);
  --color-secondaryDark: var(--color-darkTextSecondary);
}

.dark .bg-background {
  background-color: var(--color-darkBackground) !important;
}

.dark .bg-secondary,
.dark .bg-white {
  background-color: var(--color-darkSurface) !important;
}

.dark .text-secondaryDark,
.dark .text-black,
.dark .text-gray-500,
.dark .text-gray-400 {
  color: var(--color-darkTextSecondary) !important;
}

.dark .border-secondary,
.dark .border-gray-200 {
  border-color: var(--color-darkBorder) !important;
}

🔲 Ⅳ. BORDER RADIUS (TOKEN-ONLY SYSTEM)

🔹 Allowed values ONLY:

▫ rounded-small → 4px
▫ rounded-medium → 6px
▫ rounded-large → 8px
▫ rounded-veryLarge → 12px

⚠ rounded-full → ONLY for circles (avatars/icons)

🎥 Ⅴ. INTERACTION & ANIMATION RULES (UPDATED)

▶ EVERY interactive element MUST include ALL

🧲 Hover: hover:scale-[0.99]
🫳 Active: active:scale-[0.99]
🎯 Focus:
focus:outline-none focus:ring-2 focus:ring-primary/20 focus:scale-[0.99]
⏱ Transition: transition-all duration-200 ease-in-out

🚫 Missing one = INVALID COMPONENT

🔤 Ⅵ. TEXT SIZING (STRICT)

🅰 Allowed sizes only:
🔠 text-xs
🔠 text-sm
🔠 text-base
🔠 text-lg
🔠 text-xl

🚫 text-[px] forbidden

⚙️ FUNCTION & LOGIC RULES

🧠 Components & Logic
✔ Named functions ONLY

function MyComponent() {
  return ...
}


⚠ Arrow functions
✔ Allowed ONLY inside JSX handlers

onClick={() => handleAction()}


🚫 Never for components, utils, stores

🌗 GLOBAL DARK THEME STATE — ZUSTAND (REQUIRED)
🧠 ThemeStore (Persisted)
import { create } from "zustand";
import { persist } from "zustand/middleware";

const ThemeStore = create(
  persist(
    function (set) {
      return {
        isDarkTheme: false,
        toggleTheme: function () {
          set(function (state) {
            return { isDarkTheme: !state.isDarkTheme };
          });
        },
      };
    },
    { name: "theme-preference" }
  )
);

export default ThemeStore;

🧩 Example Usage (Correct Pattern)
import React from "react";
import ThemeStore from "../../store/ThemeStore";

export default function Signup() {
  const { isDarkTheme, toggleTheme } = ThemeStore();

  return (
    <div className={isDarkTheme ? "dark" : ""}>
      <div className="min-h-screen bg-background text-secondaryDark p-6 space-y-4">
        <h1 className="text-xl font-bold">Zustand testing</h1>

        <button
          onClick={toggleTheme}
          className="bg-primary text-white px-4 py-2 rounded-medium
                     hover:scale-[0.99] active:scale-[0.99]
                     focus:outline-none focus:ring-2 focus:ring-primary/20
                     transition-all"
        >
          Press me
        </button>

        <h2 className="text-lg">{isDarkTheme ? "dark" : "light"}</h2>
      </div>
    </div>
  );
}

🎪 ICON SYSTEM (UNCHANGED — FINAL AUTHORITY)

📊 Ⅺ. GRAPH / STATISTICS COMPONENT SYSTEM (NEW — REQUIRED)
Allowed Graph Components ONLY

📁 Location:

components/Statistics/
├── RadarChart.jsx
├── LineChart.jsx
└── PieChart.jsx


🚫 NO Recharts usage in pages
🚫 NO inline charts

Import Graphs
import RadarChart from "../../components/Statistics/RadarChart";
import LineChart from "../../components/Statistics/LineChart";
import PieChart from "../../components/Statistics/PieChart";

Data Contracts (STRICT)
Radar
const radarData = [
  { category: "Resolved", value: 85 },
  { category: "Pending", value: 45 },
];

Line
const trendData = [
  { week: "Week 1", complaints: 12 },
  { week: "Week 2", complaints: 19 },
];

Pie
const communityData = [
  { name: "Accepted", value: 85, color: "#1E8E54" },
  { name: "Declined", value: 15, color: "#E75A4C" },
];


🚫 No inline data
🚫 No mutation inside JSX

Using Graphs in Pages
function StatisticsPage() {
  return (
    <div className="space-y-6">
      <RadarChart data={radarData} yearDropDown={["2025", "2026"]} />

      <LineChart
        data={trendData}
        yearDropDown={["2025", "2026"]}
        monthDropDown={months}
      />

      <PieChart data={communityData} yearDropDown={["2025", "2026"]} />
    </div>
  );
}


🟢 Graphs already include:

Card wrapper

Theme tokens

Hover scale-[0.99]

Toast notifications

Dark mode support

🚫 Do NOT rewrap
🚫 Do NOT restyle
✔ Centralized import only
✔ Priority logic enforced
✔ Single Icons.jsx source of truth

🛑 FINAL ENFORCEMENT

⛔ No mixed themes
⛔ No inline styles
⛔ No gray/black utilities
⛔ No uncontrolled icons
⛔ No custom animation CSS

✅ This document overrides opinions
✅ Copy-paste safe for any AI
✅ Production-grade & future-proof